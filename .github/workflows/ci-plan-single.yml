name: Diagnostic single workflow planning
on:
  pull_request:
permissions:
  contents: read
  pull-requests: write
jobs:
  witness:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: echo 'Single local workflow loaded; this is not a release check.'
  leaf:
    if: false
    uses: ./.github/workflows/infographic-check.yml
